#include <iostream>
#include<vector>
#include<string>


using namespace std;
int main()
{
	vector<string> palabras;
	palabras.push_back("carajo);
	palabras.push_back("mierda");
	palabras.push_back("puta");
	string temp;
	cout<<"numero de palabras "<<words.size()<<endl;
	while(cin>>temp)
	{
		
	
	for (int i=0;i<words.size();i++)
		{
			if(words[i-1]!=words[i])
				cout<<words[i]<<"\n";
		}
}
